{
  "name": "samcicd",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node ./node_modules/mocha/bin/_mocha --ui tdd ./app-tests/lambda/api ",
    "sam_package": "sam build --template app.yaml && sam package --output-template-file app-packaged.yaml --s3-bucket rehan-sam-packages --s3-prefix samcicd"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.1.1",
    "nyc": "^13.2.0"
  },
  "nyc": {
    "exclude": [
      "tests"
    ]
  }
}
